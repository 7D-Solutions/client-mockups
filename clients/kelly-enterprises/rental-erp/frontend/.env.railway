# Railway Frontend Environment Configuration
# NOTE: This file is excluded from Docker builds via .dockerignore
# Railway toml provides the actual build-time environment variables

NODE_ENV=production

# API Configuration - Use empty string because client.ts appends /api automatically
# This ensures the final baseURL is /api (not /api/api)
# The infrastructure/api/client.ts file does: baseURL = `${getApiUrl()}/api`
VITE_API_URL=

# App Configuration
VITE_APP_NAME="Fire-Proof ERP"
VITE_APP_VERSION="1.0.0"

# Build Configuration
VITE_BUILD_TARGET=es2020
VITE_BUILD_OUTDIR=dist